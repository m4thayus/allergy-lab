<%= form_for @recipe do |f| %>
    <%= f.label :name, "Recipe Name:" %>
    <%= f.text_field :name %>
    <%= f.label :user_id %>
    <%= f.collection_select(:user_id, User.all, :id, :name, prompt: "What's your name?") %>
    <%= f.submit %>
<% end %>